experiment_name: "train_lasso_1"
random_seed: 42
backend: "numpy"
log_dir: "runs"
save_weights: true
weights_path: "weights"
tolerance: 0.07

epochs: 50000

dataset:
  path: "datasets/regression/bodyfat_scale.txt"
  precision: "float32" # float64 or float32
  batch_size: null # null为full batch

lasso_model:
  lam: 0.01
  sub_gradient: "off" # off, zero, random

optimizer:
  type: "optimizer.GradientDescent"
  lr: 0.01
  #nesterov: false

scheduler:
  type: "scheduler.LineSearch"
  #T_max: 500



